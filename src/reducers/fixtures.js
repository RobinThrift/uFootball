// @flow
import {createReducer} from '../utils/createReducer';

export const GET_USER_SETTINGS = 'uFootball/GET_USER_SETTINGS';

const initState = {
};

export let fixtures = createReducer({

}, initState);

